{
  "name": "babel",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild `find ./src \\( -name '*.js' \\)` --bundle --outdir=./lib"
  },
  "devDependencies": {
    "esbuild": "^0.12.29"
  }
}